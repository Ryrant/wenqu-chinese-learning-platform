# 平台空壳审计与整改记录

审计日期：2026-07-25

## P0：会误导或造成数据风险

| 原问题 | 证据 | 整改结果 |
|---|---|---|
| 所有请求硬编码到同一租户 | `request-context.ts` 固定 `demo-nanyang` | 已改为登录邮箱映射工作区，查询和写入均校验 `tenant_id` |
| 匿名请求回退为演示管理员 | `demo-admin@wenqu.local` | 已删除；匿名工作区与写操作返回 401 |
| 发音固定返回 92 分 | 前端定时器 `setScore(92)` | 已删除；真实 MediaRecorder 录音上传 R2，未配置评分服务时转人工审核 |
| 模型服务、延迟、可用率均为静态值 | 机构页“全部正常/99.98%” | 已改为读取真实绑定/环境配置，未配置显示 `template` 或 `manual` |
| 角色数据只依赖前端切换 | 同一页面显示所有硬编码数据 | 已改为服务端角色成员关系与角色范围查询；非管理员看不到审计和邀请 |

## P1：核心流程只有展示

| 原问题 | 整改结果 |
|---|---|
| 侧边导航只改变高亮 | 16 个角色导航项均切换到独立功能视图 |
| AI 备课用 1.4 秒定时器生成固定教案 | 检索已发布知识片段，保存教案、AI 会话、引用和审计记录 |
| 知识检索使用三个静态数组，并宣称“向量+重排” | 改为当前租户 D1 已发布片段检索，返回真实检索方式 |
| 创建班级、任务、发布、批阅均为提示 | 已实现创建、发布、提交、评分及状态回读 |
| 上传资料按钮没有文件选择 | 已实现类型/大小校验、R2 上传、D1 元数据、权属与发布审核 |
| 邀请成员仅显示成功提示 | 已保存邀请、角色、过期时间和令牌；受邀邮箱首次登录自动加入租户 |
| 家庭日历只显示提示 | 改为平台内持久化提醒，并明确未写入外部日历 |
| 授权管理没有数据操作 | 已实现同意/撤回和审计记录 |

## P2：质量与可观测性

- 新增 `lesson_plans`、`notifications`、`invitations` 三张表，总计 21 张业务表。
- 健康接口不再虚构供应商状态。
- 新增加载、错误、空数据、成功和待人工审核状态。
- 增加租户范围、真实操作、降级策略的自动化契约测试。

## 仍需外部条件

- 外部大模型、ASR/TTS、发音评分和内容安全供应商尚未配置密钥；界面已诚实降级，不输出伪造结果。
- 学校正式上线前仍需接入实际成员目录、监护人—学生关系和法务确认的数据保留策略。
- OpenSearch、向量库和图数据库仍属于后续基础设施阶段；当前检索明确标注为 D1 已发布内容关键词检索。
